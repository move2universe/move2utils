% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool_by.R
\name{.apply_pool_union}
\alias{.apply_pool_union}
\title{Apply a pool-fit threshold + union flags into an existing output object.}
\usage{
.apply_pool_union(out, x, pool_by, pool_step_fn, flag_cols, silent = FALSE)
}
\arguments{
\item{out}{The output \code{move2} object from per-track dispatch.
Must already carry the diagnostic columns the \code{pool_step_fn}
closure will read.}

\item{x}{The original input \code{move2} (for \code{mt_track_id} and
\code{mt_track_data}).}

\item{pool_by}{Character vector length 1 or 2 (already validated
by caller).}

\item{pool_step_fn}{Function \verb{function(fit_idx, apply_idx) -> logical(length(apply_idx))}.  Given integer event-row indices
of the OUTER group (\code{fit_idx}, the fit set) and INNER group
(\code{apply_idx}, the union target), fit the pool threshold from
\code{fit_idx}'s data and return a logical vector marking which
\code{apply_idx} events the pool threshold would flag.  Under
length-1 \code{pool_by}, \code{fit_idx} and \code{apply_idx} are the same
index set.  The closure is responsible for primitive-specific
gating (\code{legs_ok}, mode-position guard, etc.) so the union
respects all gates the per-track flagging would have applied.}

\item{flag_cols}{Character vector.  Names of logical columns
in \code{out} to union the pool flags into (e.g.
\code{c("is_outlier", "flagged_by_detour")}).  Missing columns
are silently skipped.}

\item{silent}{Logical.  Suppress the NA-fallback warning from
\code{.resolve_pool_groups}.}
}
\value{
\code{out} with \code{flag_cols} unioned in place.
}
\description{
Internal helper shared by every post-hoc threshold-fitting primitive
(\code{mt_flag_outliers_detour}, \code{mt_flag_speed_cap},
\code{mt_flag_outliers_bridge}).  The primitive's wrapper runs the
normal per-track dispatch first (producing per-track flags with
track-local thresholds), then calls this helper to layer a
pool-fitted threshold on top -- additive only, never un-flags
anything the per-track pass caught.
}
\details{
Iterates over INNER groups (the operating unit).  For each inner
group, the OUTER group it nests in supplies the threshold-fit
event set; the inner group itself supplies the events the new
threshold is evaluated against and unioned into.  Under length-1
\code{pool_by}, outer == inner and both index sets coincide --
identical to the legacy single-level behaviour.

Single-track-in-group equivalence: if an inner group contains one
track AND its outer group also contains only that track, the
pool-fit threshold is computed from that track's own diagnostic,
which is the same data the per-track threshold was computed from.
The pool union is then by construction byte-identical to the
per-track flags (assuming the \code{pool_step_fn} reproduces the per-
track gates exactly).  Pool_by never regresses a single-track
input.
}
\keyword{internal}
