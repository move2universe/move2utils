% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_mask_segments.R
\name{mt_mask_segments}
\alias{mt_mask_segments}
\title{Exclude segments from a dBBMM or dBGB utilisation distribution}
\usage{
mt_mask_segments(x, segments)
}
\arguments{
\item{x}{A variance object of class \code{mt_dbbmm_variance} or
\code{mt_dbgb_variance}, or a named list of such objects as returned by
multi-track dispatch.}

\item{segments}{Integer vector of segment indices to exclude (each in
\code{1:(n_locs - 1)}), \strong{or} a logical vector of length \code{n_locs - 1}
or \code{n_locs} where \code{TRUE} means "exclude". \code{NA} entries are ignored.}
}
\value{
The variance object (or list of objects) with the mask field
updated.
}
\description{
Mark specific track segments so that their Brownian bridges are
excluded when \code{\link[=mt_dbbmm_ud]{mt_dbbmm_ud()}} or \code{\link[=mt_dbgb_ud]{mt_dbgb_ud()}} computes the
utilisation distribution. A segment \code{i} is the bridge from location
\code{i} to location \code{i + 1}; setting it to excluded drops the
contribution of that bridge while leaving the variance estimates and
the neighbouring bridges untouched.
}
\details{
The helper is a thin dispatcher over the per-class include-masks
(\code{interest} on \code{mt_dbbmm_variance}, \code{seg_interest} on
\code{mt_dbgb_variance}). Both masks have identical semantics: \code{TRUE}
means "include this bridge in the UD", \code{FALSE} means "exclude it".
Using this helper keeps user code identical across the two variance
classes.

The common use case is removing long-gap bridges that would otherwise
fan the UD out across regions the animal never visited. See
\code{vignette("UD_gap_aware_ud", package = "move2utils")} for a worked
example on the fisher data.
}
\examples{
\dontrun{
library(move2)
library(sf)
library(units)

fishers <- mt_read(mt_example())
fishers <- fishers[!st_is_empty(fishers), ]
leroy   <- fishers[mt_track_id(fishers) == "M1", ]
leroy   <- st_transform(leroy, mt_aeqd_crs(leroy))

lag_h <- as.numeric(set_units(mt_time_lags(leroy), "hour"))
long_gap <- which(lag_h > 6)

var <- mt_dbbmm_variance(leroy, location_error = 25,
                          window_size = 31, margin = 11)
var <- mt_mask_segments(var, long_gap)
ud  <- mt_dbbmm_ud(var, location_error = 25, raster = 100, ext = 0.5)
}

}
\seealso{
\code{\link[=mt_dbbmm_variance]{mt_dbbmm_variance()}}, \code{\link[=mt_dbgb_variance]{mt_dbgb_variance()}},
\code{\link[=mt_dbbmm_ud]{mt_dbbmm_ud()}}, \code{\link[=mt_dbgb_ud]{mt_dbgb_ud()}}.
}
