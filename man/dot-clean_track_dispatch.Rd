% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_track_dispatch.R
\name{.clean_track_dispatch}
\alias{.clean_track_dispatch}
\title{Dispatch a \code{.fn_core} call from a \code{move2} object.}
\usage{
.clean_track_dispatch(
  x,
  fn_core,
  fn_core_args = list(),
  per_track_args = list(),
  pool_args_by_track = NULL,
  need_time = TRUE,
  hygiene_strict_time = TRUE,
  project_longlat = TRUE,
  n_min = 10L,
  n_min_severity = c("warning", "say"),
  primitive_label = "primitive",
  silent = FALSE
)
}
\arguments{
\item{x}{A \code{move2} object.}

\item{fn_core}{The \code{.fn_core} function to call (e.g.
\code{.bridge_fn_core}).}

\item{fn_core_args}{Named list of extra arguments to pass to
\code{fn_core} \emph{after} the standard positional ones (\code{cc}, optionally
\code{t_s}, \code{active_idx}).}

\item{per_track_args}{Named list of arguments whose \strong{numeric
vector} values need to be sliced per track during multi-track
dispatch (e.g. \code{location_error}).  Non-numeric or scalar values
pass through unchanged.  After per-track slicing, these are
merged into \code{fn_core_args} for the single-track call.}

\item{pool_args_by_track}{Named list, track id (as character) ->
a named list of arguments to inject into \code{fn_core_args} for
that track.  Used by \code{pool_by}: the wrapper fits one set of
pooled parameters per group, then builds this map so every
track in a group receives the same fitted parameters during
dispatch.  NULL (default) means no pool injection.}

\item{need_time}{Logical.  If \code{TRUE} (default), extract a
time-in-seconds vector and pass it to \code{fn_core} as the second
positional argument.  If \code{FALSE}, omit time extraction (for
geometric, time-insensitive primitives such as
\code{mt_flag_outliers_detour}).}

\item{hygiene_strict_time}{Logical.  If \code{TRUE} and \code{need_time = TRUE}, stop with an informative error when consecutive duplicate
or out-of-order timestamps are present.  If \code{FALSE}, skip the
strict timestamp checks (still excludes rows with non-finite
times from the active mask).}

\item{project_longlat}{Logical.  If \code{TRUE} (default) and the input
is in geographic coordinates, project to a local AEQD before
extracting \code{cc}; the result columns are then attached back to
the original (geographic) \code{move2} object so the caller's CRS is
preserved.  Set \code{FALSE} for primitives that handle geographic
input directly (e.g. \code{mt_flag_outliers_detour} uses Haversine
on the raw coordinates to avoid AEQD projection cost on
multi-million-fix tracks).}

\item{n_min}{Integer.  Minimum row count below which the dispatcher
emits a diagnostic (see \code{n_min_severity}) and calls \code{fn_core}
anyway (which returns all-NA output via its built-in min-active
guard).}

\item{n_min_severity}{One of \code{"warning"} (default; fires a \code{warning()}
that the user sees regardless of \code{silent}) or \code{"say"} (a
\code{message()} gated by \code{silent}).  Match the originating primitive's
pre-refactor convention: e.g. \code{mt_flag_outliers_bridge} warned,
\code{mt_flag_outliers_detour} used a quiet say-message.}

\item{primitive_label}{Character.  Short name for the primitive,
used only in diagnostic messages (\code{"bridge"}, \code{"detour"}, ...).}

\item{silent}{Logical.  Suppress per-primitive narration.}
}
\value{
The augmented \code{move2} object with \code{fn_core}'s output
columns attached (and any scalar elements of \code{fn_core}'s return
list lifted to \code{attr(x, name)}).  CRS restored to the caller's
original.
}
\description{
Internal helper.  Handles the common plumbing each outlier-
detection primitive wrapper would otherwise duplicate.
}
\keyword{internal}
