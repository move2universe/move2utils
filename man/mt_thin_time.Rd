% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_thin_time.R
\name{mt_thin_time}
\alias{mt_thin_time}
\title{Thin a track to a target time interval (tolerance-constrained)}
\usage{
mt_thin_time(
  x,
  interval,
  tolerance = NULL,
  criterion = c("closest", "first"),
  remove = FALSE
)
}
\arguments{
\item{x}{A \code{move2} object.}

\item{interval}{Target interval between successive retained fixes.
A \code{difftime}, a \code{units} object with a time dimension (e.g.
\code{units::set_units(45, "min")}), a \code{numeric} in seconds, or a string
parseable by \code{lubridate::duration()} (e.g. \code{"45 mins"}).}

\item{tolerance}{Half-width of the acceptance window around
\code{interval}, in the same units as \code{interval}. Defaults to
\code{interval / 10}.}

\item{criterion}{Tie-breaking rule when multiple chains of equal
length exist: \code{"closest"} (minimum total deviation from the
target interval, default) or \code{"first"} (earliest-starting chain).}

\item{remove}{Logical. If \code{TRUE}, return only the retained fixes.
Default \code{FALSE} — the full object is returned with the
\code{thin_selected} flag.}
}
\value{
A \code{move2} object. With \code{remove = FALSE}, the input is
returned unchanged except for a new logical column
\code{thin_selected}. With \code{remove = TRUE}, only rows with
\code{thin_selected == TRUE} are returned. For multi-track input the
thinning is performed independently per track.
}
\description{
Select a subset of fixes such that every successive retained pair has
a time lag within \verb{[interval - tolerance, interval + tolerance]}, and
such that the number of retained fixes is maximised. The original
\code{move2} object is returned whole with a logical column
\code{thin_selected} marking retained fixes.
}
\details{
This is the move2 analogue of \code{move::thinTrackTime()}. The legacy
implementation used memoised recursion on 100-fix chunks; this one
uses a linear-time dynamic-programming sweep, so it scales cleanly
to tracks with \eqn{10^5}{1e5}+ fixes.

The problem is to find the longest path in a directed acyclic graph
whose nodes are fixes and whose edges \code{i -> j} exist iff
\code{t[j] - t[i]} is within \verb{[interval - tolerance, interval + tolerance]}.
On sorted timestamps it is solved with the recursion
\deqn{f(i) = 1 + \max_{j < i, \; t_i - t_j \in [d - \tau, d + \tau]} f(j)}
followed by a single backtrace through the predecessor array.

Tracks are pre-split at any gap longer than \code{interval + tolerance};
thinning never bridges such a gap.

When several chains are equally long, \code{criterion = "closest"} (the
default) picks the chain whose summed absolute deviation from the
target interval is smallest; \code{criterion = "first"} picks the chain
that starts earliest. The \code{"all"} mode of the legacy function is not
reproduced — it can explode combinatorially and is rarely the
operationally useful choice. If you need exact parity with
\code{move::thinTrackTime(..., criteria = "all")}, call the legacy
function on a \code{move2::to_move()} conversion.
}
\examples{
\donttest{
library(move2)
fishers <- mt_read(mt_example())
fishers <- fishers[!sf::st_is_empty(fishers), ]
leroy   <- fishers[mt_track_id(fishers) == "M4", ][seq_len(500), ]

out <- mt_thin_time(leroy, interval = "45 min", tolerance = "5 min")
table(out$thin_selected)

kept <- out[out$thin_selected, ]
summary(as.numeric(mt_time_lags(kept, units = "min")))
}

}
\seealso{
\code{\link[move2:mt_filter_per_interval]{move2::mt_filter_per_interval()}} for bucket-aligned
thinning (different semantics), \code{\link[move2:mt_interpolate]{move2::mt_interpolate()}} for the
inverse problem of increasing resolution.
}
