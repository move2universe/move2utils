% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_thin_distance.R
\name{mt_thin_distance}
\alias{mt_thin_distance}
\title{Thin a track by along-track distance}
\usage{
mt_thin_distance(
  x,
  distance,
  tolerance = NULL,
  criterion = c("closest", "first"),
  method = c("step", "interval"),
  remove = FALSE
)
}
\arguments{
\item{x}{A \code{move2} object.}

\item{distance}{The target along-track distance step, in metres.
Accepts a numeric scalar or a \code{units} object convertible to metres
(e.g. \code{units::set_units(0.5, "km")}).}

\item{tolerance}{Half-width of the acceptance window around
\code{distance}, in metres. Used only by \code{method = "interval"}; ignored
(with a warning if set) by \code{method = "step"}. Defaults to
\code{distance / 10}.}

\item{criterion}{Tie-breaking rule for \code{method = "interval"} when
multiple chains of equal length exist: \code{"closest"} (minimum total
deviation from the target, default) or \code{"first"} (earliest-starting
chain). Ignored by \code{method = "step"}.}

\item{method}{Thinning rule: \code{"step"} (default, greedy minimum-spacing)
or \code{"interval"} (tolerance-constrained longest chain).}

\item{remove}{Logical. If \code{TRUE}, return only the retained fixes.
Default \code{FALSE} -- the full object is returned with a logical
\code{thin_selected} column.}
}
\value{
A \code{move2} object. With \code{remove = FALSE}, the input is
returned unchanged except for a new logical column
\code{thin_selected}. With \code{remove = TRUE}, only rows with
\code{thin_selected == TRUE} are returned.
}
\description{
Select a subset of fixes spaced by cumulative along-track distance,
returning the \code{move2} object whole with a logical column
\code{thin_selected} marking retained fixes. Two methods are offered:
}
\details{
\itemize{
\item \code{method = "step"} (default) walks the track from the first fix and
retains a fix each time the cumulative distance \strong{since the last
retained fix} reaches \code{distance}. Every retained pair is therefore
at least \code{distance} of travel apart -- the intuitive "give me one
fix per \code{distance} of travel" behaviour.
\item \code{method = "interval"} is the distance-space twin of
\code{\link[=mt_thin_time]{mt_thin_time()}}: it finds the \emph{largest} subset such that every
successive retained pair is within \verb{[distance - tolerance, distance + tolerance]} of along-track travel. This is the faithful
\code{move2} analogue of \code{move::thinDistanceAlongTrack()}, which is
itself the distance counterpart of \code{move::thinTrackTime()}.
}

Per-segment distance comes from \code{move2::mt_distance(units = "m")}, so
\code{distance} (and \code{tolerance}) are always in metres regardless of the
input CRS's linear unit. Geographic (lon/lat) data are thinned
against great-circle distance and projected data against planar
distance, so the two can differ slightly at the same metre threshold;
project to an equal-area metric CRS first if you need the result to be
identical across coordinate systems. Multi-track input is thinned
independently per individual.

For \code{method = "step"}, the first fix of each track is always retained,
and -- because the threshold is crossed mid-segment and we do not
interpolate -- the realised distance between successive retained fixes
can exceed \code{distance} (it never falls below it). Use
\code{\link[move2:mt_interpolate]{move2::mt_interpolate()}} first if you need exact-distance spacing.

For \code{method = "interval"}, the track is pre-split at any single step
longer than \code{distance + tolerance} (thinning never bridges such a
step), and the longest tolerance-admissible chain is selected by the
same dynamic-programming sweep used by \code{\link[=mt_thin_time]{mt_thin_time()}}. The first fix
is not guaranteed to be retained, and no retained pair is closer than
\code{distance - tolerance}.
}
\examples{
\donttest{
library(move2)
fishers <- mt_read(mt_example())
fishers <- fishers[!sf::st_is_empty(fishers), ]
leroy <- fishers[mt_track_id(fishers) == "M4", ][seq_len(500), ]

## one fix per ~300 m of travel (minimum spacing guaranteed)
out <- mt_thin_distance(leroy, distance = 300)
table(out$thin_selected)

## tolerance-constrained: retained pairs ~300 m +/- 30 m apart
out2 <- mt_thin_distance(leroy, distance = 300, tolerance = 30,
                         method = "interval")
table(out2$thin_selected)
}

}
\seealso{
\code{\link[=mt_thin_time]{mt_thin_time()}} for time-based thinning (same DP machinery);
\code{\link[move2:mt_interpolate]{move2::mt_interpolate()}} to first regularise the track in time or
along a target line; \code{\link[move2:mt_distance]{move2::mt_distance()}} for the underlying
distance.
}
