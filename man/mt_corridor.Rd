% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_corridor.R
\name{mt_corridor}
\alias{mt_corridor}
\title{Identify corridor segments in a move2 trajectory}
\usage{
mt_corridor(
  x,
  speed_threshold = NULL,
  circvar_threshold = NULL,
  min_segments = 2L,
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{move2} object. Multi-track input is handled track-by-track
via \code{\link[move2:mt_segments]{move2::mt_segments()}}; the input may be in any CRS (geographic
or projected). Rows with empty geometries are dropped from the
computation with an informational notice and re-padded with \code{NA}
in the output.}

\item{speed_threshold}{Numeric. Per-segment speed (metres / second) at
or above which a segment is "fast." If \code{NULL} (default), the 0.75
quantile of within-object segment speeds is used and a warning
names the resolved value.}

\item{circvar_threshold}{Numeric in \verb{[0, 1]}. Circular variance of
pseudo-azimuths within a segment's neighbourhood at or below which
the segment is "directionally consistent." If \code{NULL} (default),
the 0.25 quantile of valid within-object circular variances is
used and a warning names the resolved value.}

\item{min_segments}{Integer. Minimum number of qualifying neighbours
required for a segment to be classified as a corridor (default 2).}

\item{verbose}{Logical. If \code{TRUE}, prints a one-line summary of how
many segments were flagged. Default \code{FALSE}. Independent of the
default-threshold, irregular-sampling, and empty-geometry notices,
which are shown regardless.}
}
\value{
The input \code{move2} object with five added columns, each of
length \code{nrow(x)}:
\describe{
\item{\code{corridor}}{factor with levels \code{"corridor"} and
\code{"not corridor"}.}
\item{\code{corridor_speed}}{per-segment speed (m/s).}
\item{\code{corridor_azimuth}}{per-segment travel azimuth (degrees).}
\item{\code{corridor_circvar}}{circular variance of pseudo-azimuths
within the neighbourhood.}
\item{\code{corridor_n_neighbours}}{number of segments found within
the search radius.}
}
Track-final rows, rows with empty geometries, and segments whose
neighbourhood circular variance cannot be computed all carry \code{NA}
in the per-segment columns; their \code{corridor} factor level is
\code{"not corridor"}.
}
\description{
Detects corridor behaviour by finding segments where the animal moves
quickly in a spatially consistent direction. Corridors are characterised
by high segment speed combined with low circular variance of travel
direction among spatially neighbouring segments.
}
\details{
The algorithm:
\enumerate{
\item Build per-segment linestrings with \code{\link[move2:mt_segments]{move2::mt_segments()}} and
take their midpoints with \code{\link[sf:st_line_interpolate]{sf::st_line_interpolate()}}. Both
are track-aware; cross-track midpoints are never produced.
\item For each segment, find all other midpoints within its own
half-length search radius via an \code{sf} spatial index (R-tree).
The search uses the input CRS if projected, or a local
azimuthal-equidistant projection (\code{\link[move2:mt_aeqd_crs]{move2::mt_aeqd_crs()}}) if
the input is in longitude/latitude.
\item Compute a pseudo-azimuth (\verb{(2 * azimuth) mod 2*pi}, working
in radians) so parallel opposite directions collapse onto
the same value: animals walking north and south along the
same corridor are treated as directionally consistent.
\item Compute the circular variance of pseudo-azimuths within each
neighbourhood.
\item Flag a segment as corridor if its speed is at least
\code{speed_threshold}, its circular variance is at most
\code{circvar_threshold}, and the corridor-qualifying neighbours
within its search radius outnumber the non-qualifying ones
(at least \code{min_segments} qualifying neighbours required).
}

Thresholds are user-supplied. When either is left as \code{NULL} the
function falls back to a within-object quantile (0.75 of segment
speeds; 0.25 of valid circular variances) and emits a warning
naming the resolved numeric. For comparable corridor maps across
individuals or populations, supply explicit thresholds: e.g.
\code{speed_threshold = quantile(as.numeric(mt_speed(all_tracks, units = "m/s")), 0.75, na.rm = TRUE)} computed once on the pooled
cohort and reused per individual.

This is a port of the \code{move::corridor()} concept (LaPoint et al. 2013)
to the \code{move2} / \code{sf} stack, using modern spatial indexing.
}
\examples{
\dontrun{
library(move2)
fishers <- mt_read(mt_example())
out <- mt_corridor(fishers)
table(out$corridor)

## fixed thresholds for cross-individual comparability:
all_speeds <- as.numeric(mt_speed(fishers, units = "m/s"))
out2 <- mt_corridor(
  fishers,
  speed_threshold   = stats::quantile(all_speeds, 0.75, na.rm = TRUE),
  circvar_threshold = 0.2
)
}

}
\references{
LaPoint S, Gallery P, Wikelski M, Kays R (2013). Animal behavior,
cost-based corridor models, and real corridors. \emph{Landscape Ecology},
28, 1615–1630. \doi{10.1007/s10980-013-9910-0}
}
