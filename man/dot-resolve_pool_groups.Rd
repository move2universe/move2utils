% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool_by.R
\name{.resolve_pool_groups}
\alias{.resolve_pool_groups}
\title{Resolve \code{pool_by} into outer and inner track-id -> group-id maps.}
\usage{
.resolve_pool_groups(x, pool_by, silent = FALSE)
}
\arguments{
\item{x}{A move2 object.}

\item{pool_by}{Character vector of length 1 or 2 naming columns
in \code{mt_track_data(x)}.  Length 1: same column is used as both
outer (fit set) and inner (operating unit).  Length 2:
\code{c(outer, inner)} -- \code{outer} defines the threshold-fit
distribution source, \code{inner} defines the flag-union /
post-cascade sweep unit.  \code{inner} must nest strictly in \code{outer}:
every distinct \code{inner} value must map to exactly one \code{outer}
value.  Anything longer is rejected with the
hierarchical-estimation rationale.  Must be non-NULL; callers
gate the NULL case before invoking this helper.}

\item{silent}{Logical.  Suppress the NA-fallback warning.}
}
\value{
Named list with two elements:
\describe{
\item{\code{outer}}{Named character vector mapping track id ->
outer-group id, used as the fit-set selector.}
\item{\code{inner}}{Named character vector mapping track id ->
inner-group id, used as the union / operating-unit selector.}
}
Both maps share the same set of names (all unique track ids of
\code{x}, as character).  Under length-1 input, \code{outer} and \code{inner}
are identical.
}
\description{
Internal helper.  Validates the named column(s) exist in the
move2 object's track-level metadata, returns a two-element list
of named character maps from track id (as character) to group id
(as character).
}
\details{
Tracks whose \code{pool_by} column value is NA fall back to their own
one-track group (group id = track id) with a one-line warning.
This is applied per map (outer and inner each handle NA
independently).
}
\keyword{internal}
