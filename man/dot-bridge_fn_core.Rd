% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_flag_outliers_bridge.R
\name{.bridge_fn_core}
\alias{.bridge_fn_core}
\title{Bridge-residual scoring, raw-matrix entry point.}
\usage{
.bridge_fn_core(
  cc,
  t_s,
  active_idx,
  method = "combined",
  threshold_type = "entropy",
  threshold = NULL,
  location_error = NULL,
  residual_floor = 0,
  iterations = 3L,
  dedup_neighbours = TRUE,
  silent = TRUE
)
}
\arguments{
\item{cc}{Numeric matrix, two columns, full-track length.  Caller is
responsible for projection (Euclidean math expects a metric CRS).}

\item{t_s}{Numeric vector, full-track length.  Time in seconds.}

\item{active_idx}{Integer vector.  Row indices into \code{cc} and \code{t_s}
that participate in this call.  The caller is responsible for
excluding rows with non-finite coordinates / times and for
ensuring no duplicate or out-of-order timestamps among the
active subset.}

\item{method, threshold_type, threshold, residual_floor, iterations, dedup_neighbours}{As in \code{\link[=mt_flag_outliers_bridge]{mt_flag_outliers_bridge()}}.}

\item{location_error}{Either \code{NULL} or a numeric vector of per-fix
horizontal 1-sigma values (metres), \strong{full-track length} (the
helper indexes into it via \code{active_idx}).  \code{NA} anchors contribute
zero obs-error variance.}

\item{silent}{Logical; suppress per-iteration narration.}
}
\value{
A list of vectors aligned to \code{active_idx}:
\describe{
\item{\code{is_outlier}}{logical}
\item{\code{bridge_residual},\code{bridge_width},\code{bridge_eta},\code{bridge_eta_para},\code{bridge_eta_perp},\code{bridge_obs_inflation},\code{bridge_percentile}}{numeric}
\item{\code{bridge_iteration}}{integer}
\item{\code{S_hat}}{scalar (\code{NULL} if obs-error injection disabled)}
}

Caller lifts these to full-length output columns.
}
\description{
Internal counterpart to \code{\link[=mt_flag_outliers_bridge]{mt_flag_outliers_bridge()}}.  Operates on
raw coordinate / time matrices rather than a \code{move2} object so that
\code{\link[=mt_clean_track]{mt_clean_track()}} can cache \verb{(cc, t_s)} once and call the math
directly across iterations without re-extracting them per call.
}
\keyword{internal}
